#include "main.h"

/**
 * _islower - a function that prints numbers 1 or 0 depending on input
 * @c: first parameter
 * Description: prints all lowercase letters
 * Return: Always(0).
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
